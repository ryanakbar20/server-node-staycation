<!-- Modal -->
<div
  class="modal fade"
  id="edit-modal"
  tabindex="-1"
  role="dialog"
  aria-labelledby="exampleModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Update New Feature</h5>
        <button
          type="button"
          class="close"
          data-dismiss="modal"
          aria-label="Close"
        >
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <form action="/admin/item/add/feature?_method=PUT" method="POST" enctype="multipart/form-data">
      <div class="modal-body">
          <div class="form-group">
          
            <label for="nameBank">Name</label>
            <input
              required
              type="text"
              class="form-control name"
              id="name"
              name="name"
              placeholder="Enter Name"
            />
            <label class="mt-2" for="name">Qty</label>
            <input
              required
              type="text"
              class="form-control qty"
              id="qty"
              name="qty"
              placeholder="Enter qty"
            />
            <label class="mt-2" for="bank">Image</label>
            <input
              type="file"
              class="form-control"
              id="image"
              name="image"
              placeholder="Enter Image"
            />
          </div>
        </div>
        <div class="modal-footer">
          <input type="hidden" name="id" class="id"/>
          <input type="hidden" name="itemId" value="<%= itemId %>"/>
            <button type="button" class="btn btn-secondary" data-dismiss="modal">
                Close
            </button>
            <button type="submit" class="btn btn-primary">Save changes</button>
        </form>
      </div>
    </div>
  </div>
</div>
